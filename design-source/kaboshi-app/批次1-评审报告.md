# 批次1 评审报告

- 评审时间：2026-08-09
- 评审线程：质检线程
- 对应提交：`60ddbea`（批次1完成 9屏待审）、`aa0f884`（回填 commit hash）
- 评审范围：Page 1 下 10 个画板（首页2 / 专栏 / 颜色测试 / 打卡弹窗 / 泡泡值 / 会员中心4）
- 评审方式：逐屏 export PNG 对照原型 + 脚本扫描 fill/typography/interaction 合规性

## 结论：打回（阻断问题未修复前不进入批次2）

视觉方向正确，没有照抄原型的丑陋设计，整体风格统一。但工程合规性和完成度不达标：交互完全没连、文字样式一个没绑、颜色大量硬编码、台账没更新，且存在两屏重复复制、占位灰块、内容溢出等问题。

## 一、阻断问题（必须改）

### 1. 交互 0 连接（全批 10 屏）
脚本扫描结果：所有画板 `interactions.length = 0`。约定要求"交互以原型为准，能点哪跳哪照连"。
- 首页底部 Tab（首页/积分/服务/我的）要切页
- Banner"立即打卡"→打卡弹窗/打卡流程
- 专栏卡片、功能圆钮、商品卡片、福袋、积分明细"立即兑换"等原型里可点的元素都要连
- 打卡提示弹窗必须用 **Overlay**（打开/关闭），不是在画板里画一张带灰底的静态图
- 会员中心各屏之间的跳转（会员等级→搭子→邀请）、邀请按钮状态等

### 2. 文字样式 0 绑定（全批 10 屏）
所有文字 `typoRef = 0`，没有一个应用库的 typography 样式，全是手动字号字重。
- 必须把每个 text 的 `tokens.typography` 绑到库里对应的 Display/Title/Heading/Body/Label/Caption 样式。
- 这是"直用 token 不抄值"的红线，否则字体样式更新不生效。

### 3. 颜色大量硬编码 hex（全批 10 屏）
fill 引用 token 的比例：

| 画板 | fill 总数 | 引用 token | 硬编码 | 引用率 |
|------|-----------|------------|--------|--------|
| 首页/01 | 63 | 37 | 26 | 59% |
| 首页/02 | 42 | 26 | 16 | 62% |
| 专栏 | 61 | 42 | 19 | 69% |
| 颜色测试 | 61 | 42 | 19 | 69% |
| 打卡弹窗 | 13 | 7 | 6 | 54% |
| 泡泡值明细 | 38 | 26 | 12 | 68% |
| 会员中心/01 | 71 | 36 | 35 | 51% |
| 会员中心/02 | 37 | 23 | 14 | 62% |
| 会员中心/03 | 41 | 33 | 8 | 80% |
| 会员中心/04 | 54 | 42 | 12 | 78% |

所有硬编码的 `#252b3d/#16bfd3/#5b5ef7/#21b66f/#d63e50` 等必须改成绑定 color token 或库颜色样式，不能写死。即使值相同也要绑引用。

### 4.「颜色测试」屏与「诗得丽专栏」屏完全重复
导出对比：`测试/颜色测试` 和 `专栏/诗得丽专栏` 两屏像素级一模一样（Banner"怕掉色"、用户信息、六个功能入口、活动卡片、商品卡片全部相同）。
- 这是复制后没改内容。生产线程之前口头说"颜色测试确认是调试页跳过"，结果却做了一屏复制品。
- 处理：要么删除「颜色测试」画板，要么按原型真实的"颜色测试"内容重做。需用户确认该屏是否要做。

### 5. 台账未更新
生产线程声称"批次1完成"，但 `工作台账.md`：
- 页面清单所有状态仍是 ⬜
- "批次完成记录"表是空的
- 没有登记新增业务组件、没有填负责线程和 commit
- 违反"做完一批必须更新台账并 push"的约定。必须补齐。

### 6. 占位灰块/白块未替换
- 首页/02-积分明细：下方"BANNER轮播广告位"是灰色占位块，无内容
- 泡泡值/明细：右上角"诗得丽小诗IP形象"是空白块
- 首页/01 与专栏的商品图：粉/绿底色块 + 简笔瓶子图标，属于占位级，需换成统一风格的商品图
- 打卡弹窗的瓶子图标也是简笔占位

### 7. 内容溢出/裁切
- 会员中心/04-手机号邀请：底部"发送邀请"按钮下方的提示文字"邀请成功后双方均可获得100泡泡值奖励"紧贴屏幕底部/home 条，存在溢出风险，需核对安全区。

## 二、建议问题（应改）

1. 会员中心/03-搭子有：四个搭子全部叫"小美"，明显占位数据，应换成多样化的示例昵称。
2. 会员中心/01-会员等级：墨绿色"场景4"卡上的金色"Ds/1,280"文字对比度偏低，生产线程自己也提到要修，需落实。
3. 首页/01 Banner 轮播指示点、福袋悬浮钮位置、底部 Tab 选中态，对照原型确认样式和交互。
4. 状态栏图标（信号/wifi/电池）绘制较粗糙，建议统一用更精致的系统样式或图标组件。
5. 弹窗（打卡 TIPS）目前是把遮罩和弹窗画在同一个 390×844 画板里，正式交付应做成 Overlay：内容画板干净，弹窗作为浮层组件，原型运行时点打卡才弹出。

## 三、待确认（需用户拍板）

1. 「颜色测试」屏到底做不做？做的话真实内容是什么？不做就删掉这屏。
2. "DearSeed"（英文名）、等级名"澡堂博说/泡泡新生/香氛达人/头皮管理师"是否与原型一致？请确认这些是品牌既定文案而非自创。
3. 商品图/IP 形象的最终素材由谁提供？生产线程自行替换的图需要什么风格尺度？
4. 卡博士商城（H5 嵌入）的处理方式仍未定。

## 四、做得好的地方

- 视觉方向正确，品牌紫+青渐变、白色圆角卡片、柔和阴影、统一线性图标，比原型提升明显。
- 画板尺寸 390×844 正确，10 屏按模块分区排布、命名规范。
- 各屏结构基本完整，空状态（无搭子）、列表状态（有搭子、通讯录邀请）都有覆盖。
- 会员中心/03、会员中心/04 的 token 引用率相对较高（80%、78%），说明方法是会的，只是没贯彻全批。

## 五、整改要求

1. 修复上述全部阻断问题（1-7）。
2. 整改完重新提交并 push，更新台账状态为 🟩 并填批次记录。
3. 整改后由质检线程复审；阻断问题清零才可进入批次2。
4. 颜色和字体的 token 绑定，建议生产线程写一个批量后处理脚本，把已存在的硬编码值映射到对应 token 名统一替换，避免逐个人工改漏。
